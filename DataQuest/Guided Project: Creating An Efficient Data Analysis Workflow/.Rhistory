is_high_review = if_else(review_num >= 4, TRUE, FALSE)
)
# Analyzing The Data
We'll define most profitable book in terms of how many books there was sold.
```{r}
complete_reviews %>%
group_by(book) %>%
summarize(
purchased = n()
) %>%
arrange(-purchased)
complete_reviews %>%
group_by(book) %>%
summarize(
purchased = n()
) %>%
arrange(-purchased)
The books are relatively well matched in terms of purchasing, but "Fundamentals of R For Beginners" has a slight edge over everyone else.
© 2020 GitHub, Inc.
Terms
Privacy
Security
Status
Help
Contact GitHub
Pricing
API
Training
Blog
About
Found 39 out of 59 items
The books are relatively well matched in terms of purchasing, but "Fundamentals of R For Beginners" has a slight edge over everyone else.
© 2020 GitHub, Inc.
Terms
Privacy
Security
Status
Help
Contact GitHub
Pricing
API
Training
Blog
About
Found 39 out of 59 items
The books are relatively well matched in terms of purchasing, but "Fundamentals of R For Beginners" has a slight edge over everyone else.
© 2020 GitHub, Inc.
Terms
Privacy
Security
Status
Help
Contact GitHub
Pricing
API
Training
Blog
About
Found 39 out of 59 items
View(complete_reviews)
View(complete_reviews)
View(complete_reviews)
View(complete_reviews)
View(complete_reviews)
View(complete_reviews)
View(complete_reviews)
View(filled_books)
View(filled_books)
View(filled_books)
View(filled_books)
View(filled_books)
filled_books <- filled_books %>%
mutate(
state = case_when(
state == 'Texas' ~ 'TX',
filled_books <- filled_books %>%
mutate(
state = case_when(
state == 'Texas' ~ 'TX',
state == 'New York' ~ 'NY',
state == 'California' ~ 'CA',
state == 'Florida' ~ 'FL',
TRUE ~ state
)
)
unique(filled_books$state)
count = 1
for (col in colnames(book_reviews)){
unique_values[[count]] <- unique(book_reviews[[col]])
count <- count + 1
}
print(unique_values)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1
)
)
print(filled_books)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1
)
) %>%
mutate(
is_high_review = if_else(review >= 4)
)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1
)
) %>%
mutate(
is_high_review = if_else(filled_books$review >= 4)
)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1
)
) %>%
mutate(
is_high_review = if_else(filled_books$review >= 4, T, F)
)
print(filled_books)
filled_books <- filter(book_reviews, !is.na(book_reviews$review))
print(filled_books)
"Was removed 206 rows"
```{r}
filled_books <- filled_books %>%
mutate(
state = case_when(
state == 'Texas' ~ 'TX',
state == 'New York' ~ 'NY',
state == 'California' ~ 'CA',
state == 'Florida' ~ 'FL',
TRUE ~ state
)
)
unique(filled_books$state)
unique(filled_books$state)
```{r}
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1
)
) %>%
mutate(
is_high_review = if_else(filled_books$review >= 4, T, F)
)
print(filled_books)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
) %>%
mutate(
is_high_review = if_else(filled_books$review >= 4, T, F)
)
print(filled_books)
filled_books <- filter(book_reviews, !is.na(book_reviews$review))
print(filled_books)
"Was removed 206 rows"
```{r}
filled_books <- filled_books %>%
mutate(
state = case_when(
state == 'Texas' ~ 'TX',
state == 'New York' ~ 'NY',
state == 'California' ~ 'CA',
state == 'Florida' ~ 'FL',
TRUE ~ state
)
)
unique(filled_books$state)
unique(filled_books$state)
```{r}
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
) %>%
mutate(
is_high_review = if_else(filled_books$review >= 4, T, F)
)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
) %>%
mutate(
is_high_review = if_else(filled_books$review >= 4, T, F)
)
filled_books <- filled_books %>%
mutate(
state = case_when(
state == 'Texas' ~ 'TX',
state == 'New York' ~ 'NY',
state == 'California' ~ 'CA',
state == 'Florida' ~ 'FL',
TRUE ~ state
)
)
unique(filled_books$state)
unique(filled_books$state)
```{r}
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
) %>%
mutate(
is_high_review = if_else(filled_books$review >= 4, T, F)
)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
) %>%
mutate(
is_high_review = if_else(filled_books$review >= 4, T, F)
)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
) %>%
mutate(
is_high_review = if_else(filled_books$review >= 4, T, F)
)
filled_books <- filled_books %>%
mutate(
state = case_when(
state == 'Texas' ~ 'TX',
state == 'New York' ~ 'NY',
state == 'California' ~ 'CA',
state == 'Florida' ~ 'FL',
TRUE ~ state
)
)
unique(filled_books$state)
print(filled_books)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5L,
review == 'Great' ~ 4L,
review == 'Good' ~ 3L,
review == 'Fair' ~ 2L,
review == 'Poor' ~ 1L,
T ~ review
)
) %>%
mutate(
is_high_review = if_else(filled_books$review >= 4, T, F)
)
print(filled_books)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5L,
review == 'Great' ~ 4L,
review == 'Good' ~ 3L,
review == 'Fair' ~ 2L,
review == 'Poor' ~ 1L,
T ~ review
)
) %>%
mutate(
is_high_review = if_else(filled_books$review >= 4, T, F)
)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
)
print(filled_books)
filled_books <- filled_books %>%
mutate(
review = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
)
print(filled_books)
filled_books <- filled_books %>%
mutate(
review_num = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
)
filled_books <- filled_books %>%
mutate(
review_num = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
)
print(filled_books)
filled_books <- filled_books %>%
mutate(
review_num = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
)
filled_books <- filled_books %>%
mutate(
review_num = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
),
filled_books <- filled_books %>%
mutate(
review_num = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
T ~ review
)
)
filled_books <- filled_books %>%
mutate(
review_num = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
TRUE ~ review
)
)
filled_books <- filled_books %>%
mutate(
state = case_when(
state == 'Texas' ~ 'TX',
state == 'New York' ~ 'NY',
state == 'California' ~ 'CA',
state == 'Florida' ~ 'FL',
TRUE ~ state
)
)
unique(filled_books$state)
unique(filled_books$state)
```{r}
filled_books <- filled_books %>%
mutate(
review_num = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
TRUE ~ review
)
)
filled_books <- filled_books %>%
mutate(
review_num = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
)
)
print(filled_books)
filled_books <- filled_books %>%
mutate(
review_num = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
)
is_high_review = if_else(review_num >= 4, TRUE, FALSE)
filled_books <- filled_books %>%
mutate(
review_num = case_when(
review == 'Excellent' ~ 5,
review == 'Great' ~ 4,
review == 'Good' ~ 3,
review == 'Fair' ~ 2,
review == 'Poor' ~ 1,
),
is_high_review = if_else(review_num >= 4, TRUE, FALSE)
)
print(filled_books)
profit <- filled_books %>% group_by(book) %>% sum()
profit <- filled_books %>% group_by(book) %>% summarise(profit = sum(price))
profit <- filled_books %>% group_by(book) %>% summarise(profit = sum(price))
profit <- filled_books %>% group_by(book) %>% summarise(profit = n(price))
profit <- filled_books %>% group_by(book) %>% summarise(profit = n())
filled_books %>% group_by(book) %>% summarise(profit = n())
profit_book <-filled_books %>% group_by(book) %>% summarise(profit = n())
filled_books %>% group_by(book) %>% summarise(profit = n())
filled_books %>% group_by(book) %>% summarise(profit = n()) %>% arrange(-profit)
filled_books %>% group_by(book) %>% summarise(profit = sum()) %>% arrange(-profit)
filled_books %>% group_by(book) %>% summarise(profit = n()) %>% arrange(-profit)
filled_books %>% group_by(book) %>% summarise(profit = sum(price)) %>% arrange(-profit)
